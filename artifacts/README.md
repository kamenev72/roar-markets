# Reproducible artifacts

This directory keeps proof material and fixed integration inputs separate from source code.

- `evidence/` — historical receipts, UI captures, and bundle baselines.
- `fixtures/` — pinned venue binaries and replay inputs.

The product application lives in `app/`; technical explanations and limitations live in `docs/`.
