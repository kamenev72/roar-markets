node_modules/
app/test-results/
dist/
*.tsbuildinfo

# secrets / host-local — NEVER commit (the wallet keypair, the TxLINE guest JWT, the X-Api-Token are runtime-only)
.env
.env.*
*keypair*.json
*-keypair.json
id.json
secrets/
# name-agnostic key catch-alls (a signer under any of these names is ignored even if mis-named)
*wallet*.json
*signer*.json
*admin*.json
*devnet*.json
wc_devnet*.json
*key.json
keys/
*creds*.json

# Rust / OS / editor (a lost newline once folded these into a comment — see the guard)
target/
.DS_Store
.idea/
.vscode/
.vercel
.env*
