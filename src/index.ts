// PROPCAST — auto-spawned, trustlessly-settled goal-grain micro-prop markets on Solana.
//
// W1 (this checkpoint) lands the repo scaffold + clean-room gate. The vendored pricing/venue modules, the
// market_id[32] contract, and the PropMarketFactory spine arrive in the following W1 checkpoints; the live
// scores-schema pin, the on-chain settle path, and the fan UI follow in W2/W3.

/** Repo build marker — replaced by the real public API as the W1 factory spine lands. */
export const PROPCAST = "propcast" as const;
