// Roar Markets — goal-grain micro-market factory and bound-receipt verifier on Solana.
//
// phase 1 (this checkpoint) lands the repo scaffold + clean-room gate. The vendored pricing/venue modules, the
// market_id[32] contract, and the PropMarketFactory spine arrive in the following phase 1 checkpoints; the live
// scores-schema pin, the on-chain settle path, and the fan UI follow in a later phase.

/** Repo build marker — replaced by the real public API as the phase 1 factory spine lands. */
export const PROPCAST = "propcast" as const;
